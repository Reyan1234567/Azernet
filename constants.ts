export enum BusinessCashType {
  PURCHASE = "purchase",
  SALE = "sale",
  RECURRINGFEE = "recurringFee",
  PURCHASEREVERSAL = "purchaseReversal",
  SALEREVERSAL = "saleReversal",
}

export enum InventoryTypes {
  IN = "in",
  OUT = "out",
}

export enum ORDERSTATUS {
  PENDING = "pending",
  PURCHASED = "purchased",
  DELIVERED = "delivered",
}

export const bigNumber=9999999999999;
